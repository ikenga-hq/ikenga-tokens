# @ikenga/tokens

## 0.5.0

### Minor Changes

- 4e249ac: Add workspace tint tokens for the new rail nouns: `--tint-project-{bg,fg}`,
  `--tint-chi-{bg,fg}`, `--tint-ngwa-{bg,fg}` (dark + light mode), plus
  `[data-workspace="project|chi|ngwa"]` selectors resolving `--tint-bg-active`
  / `--tint-fg-active`. Values borrow the existing `files` (→ project),
  `agents` (→ chi), and `app` (→ ngwa) tints — same colors, new names. The
  old tint names are unchanged and still resolve for pkgs keyed on them.
  All 6 new `--tint-*-fg` values measure ≥ 4.5:1 against their paired
  `--tint-*-bg` across themes A/B/C and both modes (18/18 rows pass).

## 0.4.1

### Patch Changes

- 2caaea2: chore: repoint org references at ikenga-hq (#8)

## 0.4.0

### Minor Changes

- 6c102db: Add `--info` / `--info-soft` cool-blue semantic role. `--info` already existed in the base palette but was undefined under Theme A (Dusk Wood), where it fell back to a jarring pure-cyan `hsl(200,90%,60%)`; it is now Dusk-Wood-harmonized (`hsl(200,66%,64%)` dark / `hsl(200,74%,36%)` light) and gains a `--info-soft` deep tint. This is the shared cool role Studio's rendering-status (`◐`, progress bars, is-running, now-rendering beacon) and `--beat-accent-sky` re-base onto (studio-design-system G-02). WCAG-verified: 8.4:1 on `--bg-surface` (dark), all six Studio beat accents pairwise ΔE ≥ 30.

## 0.3.0

### Minor Changes

- Add the Atelier app-kit component layer + no-build distribution.
  - **New opt-in exports:** `@ikenga/tokens/app-kit.css` (the component layer) and the generated JS-string exports `@ikenga/tokens/tokens-css` + `@ikenga/tokens/app-kit-css` for no-build `srcdoc` pkgs (injected as an inline `<style>`; replaces the per-pkg hand-copied CSS-as-JS mirrors that drifted). Generated by `scripts/build-css.mjs` via a single `JSON.stringify` escape.
  - **Reconciled tokens (additive)** from the prototype `_shared` superset the locked panes consume: the `data-density` system + chrome heights (`--row-h`/`--tab-h`/`--btn-h*`/`--input-h`, with `[data-density=compact|spacious]`), `--motion-*`/`--ease-*`, `--space-10..20`, an extended type scale (`--text-body-sm/-lg/-micro/-code/-h4/-display*` + `--lead-*`), `--font-body`, `--fg-faint` in the base palette, `--live`/`--live-soft`/`--live-fg` + `--agent`/`--agent-soft` + `--achievement-soft` in Theme A (and base fallbacks for B/C). These retire the per-pkg `aliasCss` shim.
  - **Adopted the Fraunces (display) + JetBrains Mono fonts the shell already loads;** `--font-sans` (Plus Jakarta Sans) is unchanged.
  - **No change** to existing `tokens.css` color, type-_size_, or radius values — `./tokens.css` consumers (the marketing site, the shell) are visually unaffected.

## 0.2.2

### Patch Changes

- d1172bb: Adopt Changesets for versioning + release. Version and CHANGELOG are now
  derived from per-PR changeset entries and applied by CI on merge of the
  "Version Packages" PR, replacing the previous tag-triggered flow.
